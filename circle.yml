machine:
  node:
    version: 5.1.0

dependencies:
  override:
    - npm install

test:
  override:
    # Detect errors and potential problems
    #- npm run eslint -- --reset --eslintrc ./
    #- npm run jscs -- ./
    #- npm run nsp -- check

    # Run available tests
    - npm test
